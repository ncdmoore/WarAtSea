package com.enigma.waratsea.model;

/**
 * This is needed because Twilight turns are sometimes treated as day turns and sometimes treated as night turns.
 */
public enum VisibilityType {
  DAY,
  NIGHT
}
